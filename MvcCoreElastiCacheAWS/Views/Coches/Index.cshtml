@model IEnumerable<MvcCoreElastiCacheAWS.Models.Coche>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Modelo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imagen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Marca)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Modelo)
                </td>
                <td>
                    <img src="@item.Imagen" style="width:auto; max-width:300px;" />
                </td>
                <td>
                    <a class="btn btn-danger"
                       asp-controller="Coches"
                       asp-action="Details"
                       asp-route-id="@item.IdCoche">
                        DETAILS
                    </a>
                    <a class="btn btn-danger"
                       asp-controller="Coches"
                       asp-action="SeleccionarFavorito"
                       asp-route-idcoche="@item.IdCoche">
                        ⭐
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
